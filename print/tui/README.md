# Terminal printing

The `dices-print` crate produces a fully annotated document.

Here is the code to render that with ANSI sequences - toward any `io::Write`.

Needs the consumer to specify the prompt parts for the examples render.
