# HTML printing

The `dices-print` crates produces a fully annotated document, with a clear
mapping to an HTML DOM.

This crate provides a writer alternative to the CLI one to print that document
in an HTML format.
