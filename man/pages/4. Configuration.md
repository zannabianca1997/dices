`dices` is fully configurable through config files and theme files. While it
comes with a full usable configuration, it also reads additional files from the
user config directory and override the defaults with those.

The configuration is split in two parts: the *config*, that holds every
persistent setting, and the *themes*, CSS files that decide how each piece of
output is painted on your terminal. Yeah, CSS in the terminal, cursed I know.
