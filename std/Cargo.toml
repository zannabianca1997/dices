[package]
name = "dices-std"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
dices-values = { workspace = true, features = ["macros"] }

chrono = "0.4.45"

[lints]
workspace = true
